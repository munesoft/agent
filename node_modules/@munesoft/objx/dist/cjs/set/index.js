"use strict";
/**
 * @munesoft/objx — set
 * Fast, safe deep property setter with immutable mode. lodash.set alternative.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.set = set;
const index_js_1 = require("../path/index.js");
const types_js_1 = require("../utils/types.js");
/**
 * Set a deeply nested value at the given path, creating missing intermediaries.
 *
 * @param obj     - Source object
 * @param path    - Dot/bracket path string
 * @param value   - Value to set
 * @param options - { immutable: true } returns a new object without mutating
 *
 * @example
 * set({}, "a.b.c", 42)                          // { a: { b: { c: 42 } } }
 * set({ users: [] }, "users[0]", "Alice")        // { users: ["Alice"] }
 * set(obj, "a.b", 1, { immutable: true })        // new object, original untouched
 */
function set(obj, path, value, options = {}) {
    const segments = (0, index_js_1.parsePath)(path);
    if (segments.length === 0)
        return obj;
    if (options.immutable) {
        return setImmutable(obj, segments, value);
    }
    return setMutable(obj, segments, value);
}
/** Mutating set — modifies obj in place */
function setMutable(obj, segments, value) {
    let current = obj;
    for (let i = 0; i < segments.length - 1; i++) {
        const key = segments[i];
        const nextKey = segments[i + 1];
        const nextIsIndex = typeof nextKey === "number";
        let next = current[key];
        if (next == null || typeof next !== "object") {
            next = nextIsIndex ? [] : {};
            current[key] = next;
        }
        current = next;
    }
    current[segments[segments.length - 1]] = value;
    return obj;
}
/** Immutable set — returns a structurally-shared clone */
function setImmutable(obj, segments, value) {
    const key = segments[0];
    if (segments.length === 1) {
        if (Array.isArray(obj)) {
            const arr = obj.slice();
            arr[key] = value;
            return arr;
        }
        return { ...((0, types_js_1.isPlainObject)(obj) ? obj : {}), [key]: value };
    }
    const nextSegments = segments.slice(1);
    const nextKey = segments[1];
    const nextIsIndex = typeof nextKey === "number";
    let child;
    if (Array.isArray(obj)) {
        child = obj[key];
    }
    else if ((0, types_js_1.isPlainObject)(obj)) {
        child = obj[key];
    }
    else {
        child = undefined;
    }
    // Create missing intermediary
    if (child == null || typeof child !== "object") {
        child = nextIsIndex ? [] : {};
    }
    const updatedChild = setImmutable(child, nextSegments, value);
    if (Array.isArray(obj)) {
        const arr = obj.slice();
        arr[key] = updatedChild;
        return arr;
    }
    return { ...((0, types_js_1.isPlainObject)(obj) ? obj : {}), [key]: updatedChild };
}

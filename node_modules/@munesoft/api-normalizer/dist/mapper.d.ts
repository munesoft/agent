import { DiffEntry, NormalizerOptions, Schema } from "./types";
export interface MapResult {
    normalized: Record<string, unknown>;
    diff: DiffEntry[];
    missingRequired: string[];
}
/**
 * Map a single plain object against a schema.
 * Returns the normalized object plus diff and validation metadata.
 */
export declare function mapObject(data: unknown, schema: Schema, options?: NormalizerOptions): MapResult;
/**
 * Infer a basic schema from a sample object.
 * Each top-level key becomes a schema entry pointing to itself.
 * Nested objects use dot-notation paths.
 */
export declare function inferSchema(data: unknown, prefix?: string, depth?: number): Schema;
//# sourceMappingURL=mapper.d.ts.map